phase = "Phase 02 — lifecycle skills"
position = "The five lifecycle skills are written (skills/{plan,audit,apply,wrap,resume}, 17.9KB total vs PAUL's 62KB for the same five). 49 -> 55 tests green; 6 new tests lock frontmatter, the 6000-byte skill budget, and the no-args-to-flux-check invariant. Not yet exercised on a real phase."
next = "Phase 02 — migrate kiosk PAUL state into .flux/ and archive .paul/, then run one full real phase (plan -> audit -> apply -> wrap) in kiosk"
routing = "design"
open = "The five skills have never been run end-to-end; the kiosk phase is their first real test. Plugin cache is a COPY — refresh the install before testing the new skills live."
updated = "2026-08-20 23:02"
