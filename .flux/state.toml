phase = "Phase 01 — core CLI + plugin scaffold"
position = "plugin installed (flux@flux-market, directory source ~/Dev/flux, but cached as a COPY at ~/.claude/plugins/cache/flux-market/flux/2.0.0 — repo edits need a plugin update to go live); SessionStart prime verified: fires here, silent no-op in a non-flux dir"
next = "adopt flux in zaps/kiosk — flux init, propose the check command, run it once to verify, commit flux.toml, seed state from PAUL position"
routing = "design"
open = ""
updated = "2026-08-20 18:53"
