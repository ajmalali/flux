phase = "Phase 02 closed. ADR 0001 (execution index) SUSPENDED before implementation -- its own ledger metric was measured and does not support it. Nothing of flux task is built."
position = "Cold-start ramp measured on 175 real sessions (129 reaching an edit): median 21 tool calls / ~48k tokens before the first Edit. But 60% is reading source, and the frontier an index replaces is 15% of calls / 31% of result tokens -- median 3 calls, 2715 tokens, 5.7% of the ramp. 75% of ALL frontier tokens are one repo (kiosk, PAUL STATE.md 299KB read whole); everywhere else it is 1 call / 1098 tokens. flux prime already replaces that read. So ADR 0001's falsifier fired in advance. beads (bd) declined: it is a peer, not a backend, and it breaks zero-install. Shipped bench/fluxbench/ramp.py + ./bench/run.py ramp + 8 tests (150 green)."
next = "Run ONE real kiosk work session on flux, then ./bench/run.py ramp. kiosk has had zero sessions since adopting flux on 2026-08-20 -- the repo carrying 75% of the frontier cost has never met the capability meant to remove it. If its 14,083 frontier tokens collapse toward 1,098, prime took the prize and the index stays suspended for good."
routing = "build"
open = "kiosk PR #66 is open and unmerged, awaiting the user's sign-off. .flux/state.toml is tracked and rewritten every session — in kiosk's 49-branch repo it will conflict on every branch; undecided. Live skill/CLI changes need a plugin version bump before they reach the cache. The correctness-vs-context question is OPEN and unmineable: it needs a designed run (same task at deliberately different context loads, graded on acceptance) — this account is underpowered 14x for it."
updated = "2026-08-22 15:19"
