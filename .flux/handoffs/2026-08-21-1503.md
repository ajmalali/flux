# handoff — 2026-08-21 15:03

branch: main

## state
- phase: Phase 02 closed — lifecycle five exercised on kiosk; the defect that run surfaced is fixed
- position: prime now derives drift as two legs: the upstream leg ("N unpushed") answers whether work has left the laptop, the default-branch leg ("N ahead of origin/main") answers how far the branch has come, and either is omitted when zero. This replaces the single-base version shipped 2026-08-20, which went silent on a feature branch the moment it gained an upstream. 73 tests green, plugin 2.3.0.
- next: Phase 03 — generalize to zaps/api, retire the PAUL/mattpocock installs, and take the first before/after ledger reading
- routing: design
- open: kiosk PR #66 is open and unmerged, awaiting the user's sign-off. .flux/state.toml is tracked and rewritten every session — in kiosk's 49-branch repo it will conflict on every branch; undecided. Live skill/CLI changes need a plugin version bump before they reach the cache.
- updated: 2026-08-21 15:03

## working tree
```
 M .claude-plugin/plugin.json
 M .flux/plans/flux-v2/status.md
 M .flux/state.toml
 M bin/flux
 M tests/test_flux_cli.py
```

## recent commits
```
ed42b10 Phase 02 closed: the lifecycle five, run for real on kiosk
abc9db2 prime: derive branch drift live instead of storing it in position
23d23ee /flux:adopt proven on kiosk — and two defects it exposed
bcb6199 plugin 2.1.0 — publish the six skills to the marketplace
4f1d6ce /flux:adopt — bring any repo's existing knowledge into flux
bf02804 Phase 02: the five lifecycle skills, distilled from PAUL
9fafea8 init: generalize gate detection, and stop proposing scoped gates
ff9b14a kiosk adopted; found: init's nx detector proposes a scoped gate
4f5ca9f Phase 01 closed: plugin installed, SessionStart prime verified live
98193b1 flux run --filter: check's failure extractor, reusable for scoped runs
```
