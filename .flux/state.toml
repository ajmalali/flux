phase = "Phase 03 rescoped to 'establish where flux pays'. api line CLOSED as a pre-registered null; api is not adopted."
position = "flux's value is repo-shaped, binding in plan.md ('Where flux pays'): a bloated state artifact read whole + a fan-out gate. api has neither (pre-registered null). kiosk has both, fully landed (PR #66, e8a3199). The mattpocock install is RETIRED (07365f6) on a utilisation bar derived from flux's own 2,000-tok prime budget — corrected today to 6.7x, not 10.6x. That bar has now been turned on flux itself: flux:review, the ONLY skill flux listed, had 0 invocations in 520 transcripts at 109 tok/session, so it is delisted (dmi:true, applied in sync-vendored.sh since upstream has no key) and flux now ships NO model-visible skills. CAVEAT that must travel with it: the bar is ambiguous here and the reading decides it — 'any skill in the namespace' would PASS on the bench-inclusive denominator; 'only the skills it lists' governs and fails everywhere. Re-runnable: scripts/skill-utilisation.py. Plugin 2.8.0, 153 tests green."
next = "DO THIS FIRST, it verifies last session's action: run 'claude plugin update flux@flux-market' (2.8.0 — the bump is what makes an update take) and confirm on the next session that NO flux skill appears in the roster. If flux:review is still listed, the delisting did not land where 83d5012 claims. THEN the queued task: measure the AGENT roster (flux-explorer + flux-verifier, ~473 B = 118 tok/session, 0 real invocations post-install) against the same 2,000-tok bar — full brief in status.md's queue, including the finding that the agent roster is NOT recorded in transcripts (493 skill_listing attachments, zero agent equivalents), so the numerator is a projection or an n=1 read, which must be stated as the weakness it is. Pre-register the escalation first: agents have no disable-model-invocation, so 'carry but don't list' is unavailable. Phase 03's kiosk before/after ledger still accrues passively (after-side n=1) and is not an on-demand action. Then STOP auditing context: the standing build item is the append-only JSONL-in-git state format, UNBUILT."
routing = "design"
open = "carrying a skill is nearly free, LISTING it is not — and flux now applies that to itself: 0 of 14 skills are model-visible, prime is the only always-on context it buys. A listing slot is not a default; it needs a written falsifiable claim that the model must SEE the thing. Watch for inherited visibility: review was listed only because upstream's frontmatter was, which is why hide_skill lives in sync-vendored.sh. Count BOTH invocation paths (Skill tool + <command-name>) and bill only sessions that CARRIED the listing — those two errors put the mattpocock bar at 10.6x when it was 6.7x. claude plugin details is a static projection (bills dmi skills; unchanged at ~1,246 before and after the delisting). The agent roster is the last uncapped always-on path and cannot be corpus-measured. sync-vendored.sh's $SRC is orphaned — pass a marketplace checkout as $1. The append-only state format (JSONL-in-git, from bd) is UNBUILT. The ceremony question is settled by cost, not quality. The correctness-vs-context question stays OPEN. The ramp corpus erodes daily. zaps/api still has chore/retire-competing-frameworks unpushed."
updated = "2026-08-23 18:33"
