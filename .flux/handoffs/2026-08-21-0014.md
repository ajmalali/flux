# handoff — 2026-08-21 00:14

branch: main

## state
- phase: Phase 02 closed — the five lifecycle skills have now been exercised end-to-end on kiosk
- position: plan → audit → apply → wrap ran for real on zaps/kiosk phase 01: PR #66 open, CI green, not merged (user signs off). The audit returned 3 blocking / 9 recommended / 4 deferred on a plan that looked fine — including a false claim it would have written into the PR body — and folding them in doubled the plan file. Gate output 946 lines → 1. Two candidates open on flux itself: prime hides drift-vs-main once a branch has an upstream, and .flux/state.toml conflicts across a 49-branch repo.
- next: Decide the prime drift-base candidate (report against the default branch even when an upstream exists), then Phase 03 — generalize to zaps/api, retire the PAUL/mattpocock installs, and take the first before/after ledger reading
- routing: design
- open: kiosk PR #66 is open and unmerged, awaiting the user's sign-off. .flux/state.toml is tracked and rewritten every session — in kiosk's 49-branch repo it will conflict on every branch; undecided. Live skill/CLI changes need a plugin version bump before they reach the cache.
- updated: 2026-08-21 00:14

## working tree
```
 M .flux/plans/flux-v2/status.md
 M .flux/state.toml
```

## recent commits
```
abc9db2 prime: derive branch drift live instead of storing it in position
23d23ee /flux:adopt proven on kiosk — and two defects it exposed
bcb6199 plugin 2.1.0 — publish the six skills to the marketplace
4f1d6ce /flux:adopt — bring any repo's existing knowledge into flux
bf02804 Phase 02: the five lifecycle skills, distilled from PAUL
9fafea8 init: generalize gate detection, and stop proposing scoped gates
ff9b14a kiosk adopted; found: init's nx detector proposes a scoped gate
4f5ca9f Phase 01 closed: plugin installed, SessionStart prime verified live
98193b1 flux run --filter: check's failure extractor, reusable for scoped runs
5ee8b7a flux init hardening: detected check command is a candidate until first full pass
```
