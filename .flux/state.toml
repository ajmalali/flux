phase = "Phase 02 closed; benchmark meridian-003 in flight on the fixed harness"
position = "meridian-002 was spoiled by a rate limit and the harness scored it as arm failure — 32 sessions returned 429 in under a second and the report credited paul and flux-lite with 0/4 deliveries they were never given the chance to attempt. Fixed: retry 429/5xx on backoff when the attempt cost nothing, void the task otherwise, and exclude void tasks from every denominator instead of scoring them zero. Re-read honestly, 002 says vanilla 3/3 at $1.11/task vs flux 2/3 at $2.83 over m1-m3; flux lost m3 on merit. 114 tests green."
next = "Read ./bench/run.py report meridian-003, then act on the verdict table — the question is whether flux's 2.6x cost is the machinery or the ceremony (flux-lite is the arm that answers it)"
routing = "design"
open = "kiosk PR #66 is open and unmerged, awaiting the user's sign-off. .flux/state.toml is tracked and rewritten every session — in kiosk's 49-branch repo it will conflict on every branch; undecided. Live skill/CLI changes need a plugin version bump before they reach the cache."
updated = "2026-08-21 17:54"
