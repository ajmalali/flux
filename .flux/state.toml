phase = "Phase 03 rescoped to 'establish where flux pays'. api line CLOSED as a pre-registered null; api is not adopted."
position = "flux's value is repo-shaped, and that is binding in plan.md ('Where flux pays'): it needs a bloated state artifact read whole and a fan-out gate. api has neither (pre-registered null, not adopted). kiosk has both, and is now fully landed there — PR #66 merged 2026-08-23 (e8a3199), plus f293528 (codegraph hook guard) and 19861ee (state.toml untracked) straight on main. 153 tests green."
next = "Phase 03 remaining: (a) the kiosk before/after ledger accrues one datapoint per real kiosk session — after-side is still n=1 (before 40 sessions / 6 frontier calls / 15,770 tok vs after 1 / 1 / 51); re-run ./bench/run.py ramp when more land, do not treat it as an on-demand action. (b) Retire the mattpocock install, but measure the duplicate-instruction cost first. (c) 27 unpushed commits here."
routing = "design"
open = "The append-only state format (JSONL-in-git, stolen from bd) is the better long-term answer to the state.toml conflict problem and is UNBUILT — kiosk is untracked instead, which is reversible but keeps state per-clone. The ceremony question is settled by cost, not by quality: the only experiment that could test its actual claim is a terse-brief task pinned through seed API only, and it has not been built. The correctness-vs-context question stays OPEN and unmineable. The ramp corpus erodes daily — pre-prime baselines cannot be re-measured later. zaps/api still has chore/retire-competing-frameworks unpushed (193 files / -32,927 lines)."
updated = "2026-08-23 17:48"
