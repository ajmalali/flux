phase = "Phase 03 rescoped to 'establish where flux pays'. api line CLOSED as a pre-registered null; api is not adopted."
position = "flux's value is repo-shaped, binding in plan.md ('Where flux pays'): it needs a bloated state artifact read whole and a fan-out gate. api has neither (pre-registered null, not adopted). kiosk has both and is fully landed there — PR #66 merged (e8a3199). The mattpocock install is RETIRED (07365f6): a utilisation bar derived from flux's own 2,000-tok prime budget, pre-registered in ecfcffb, failed at 21,110 tok per session-of-use (10.6x) on 9 invoking sessions of 287; trim failed too. research + writing-for-agents vendored with disable-model-invocation so nothing is lost. Plugin 2.7.0. 153 tests green."
next = "Phase 03 remaining is ONE thing: the kiosk before/after ledger, which accrues one datapoint per real kiosk session — after-side still n=1 (before 40 sessions / 6 frontier calls / 15,770 tok vs after 1 / 1 / 51). Re-run ./bench/run.py ramp when more have landed; it is not an on-demand action. New and unqueued: the utilisation bar points at flux's OWN skill listing, which is uncapped and unmeasured — measure flux-skill utilisation before deciding anything there. Also: 2 unpushed commits here."
routing = "design"
open = "carrying a skill is nearly free, LISTING it is not — the rule the mattpocock retirement settled; it separates 'who owns this capability' from 'must the model see it', and vendoring at disable-model-invocation is how. claude plugin details is a static projection, not a measurement: it bills disable-model-invocation skills (flux's roster shows 1 of 14). sync-vendored.sh's $SRC survived the uninstall but is orphaned — pass a marketplace checkout as $1. The append-only state format (JSONL-in-git, from bd) is the better answer to the state.toml conflict problem and is UNBUILT. The ceremony question is settled by cost, not quality. The correctness-vs-context question stays OPEN and unmineable. The ramp corpus erodes daily. zaps/api still has chore/retire-competing-frameworks unpushed."
updated = "2026-08-23 18:09"
