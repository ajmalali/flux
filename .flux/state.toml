phase = "Phase 02/03 — adoption proven on kiosk; one full lifecycle phase still unrun"
position = "prime now derives branch drift live (ahead/behind vs upstream, else the default branch), so stored position no longer carries self-invalidating tree facts; wrap and adopt say so explicitly. Plural fix on the dirty-file count. 69 tests green, plugin 2.2.0 so the cache picks it up."
next = "Run one full lifecycle phase (plan -> audit -> apply -> wrap) in kiosk — the five skills have still never been exercised; the obvious candidate is pushing chore/retire-gitnexus-for-codegraph and opening its PR"
routing = "design"
open = "The lifecycle five (plan/audit/apply/wrap) are still unrun end-to-end. Live skill/CLI changes need a plugin version bump before they reach the cache — claude plugin update compares versions, not commits."
updated = "2026-08-20 23:57"
