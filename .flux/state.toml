phase = "Phase 03 rescoped to 'establish where flux pays'. api line CLOSED as a pre-registered null; api is not adopted."
position = "flux's value is repo-shaped, and that is now binding in plan.md ('Where flux pays'). Both measurable surfaces need a specific shape: a bloated state artifact read whole (kiosk 299 KB STATE.md -> 15,770 frontier tok; api 33 KB, mostly unread -> 1,111) and a fan-out gate (kiosk nx run-many ~950 lines; api jest/eslint 2,166 tok median, missing the 5,000 bar set before the measurement). api's state lives in GitHub Issues, not a file. 193 files / -32,927 lines of competing frameworks retired there instead, on branch chore/retire-competing-frameworks, unpushed. 153 tests green."
next = "Phase 03 remaining: retire the mattpocock install; take the first ledger before/after IN KIOSK (api cannot supply one); close kiosk PR #66 and home the uncommitted .claude/settings.json codegraph guard."
routing = "design"
open = "kiosk PR #66 open and unmerged, awaiting the user's sign-off — kiosk's tree also carries an uncommitted one-line guard on .claude/settings.json (codegraph hook 127 fix) that needs a home. .flux/state.toml is tracked and rewritten every session; in kiosk's 49-branch repo it will conflict on every branch; undecided. The ceremony question is settled by cost, not by quality: the only experiment that could test its actual claim is a terse-brief task pinned through seed API only, and it has not been built. The correctness-vs-context question stays OPEN and unmineable. The ramp corpus erodes daily — pre-prime baselines cannot be re-measured later."
updated = "2026-08-23 15:38"
